import { PenalizationOrokorra } from './penalization-orokorra';

describe('PenalizationOrokorra', () => {
  it('should create an instance', () => {
    expect(new PenalizationOrokorra()).toBeTruthy();
  });
});
