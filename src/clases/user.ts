export class User {
    id: number;
    name: string;
    puntos: number;
    constructor(id: number, name: string , puntos: number) {
        this.id = id;
        this.name = name;
        this.puntos = puntos;
    }
}
